# ReflectRAG
A stateful, self-correcting Corrective RAG (CRAG) implementation powered by LangGraph. Leverages an autonomous agent pipeline that uses structured LLM evaluators to grade retrieved documents, rewrite suboptimal queries, and execute live web-search fallbacks to eliminate hallucinations.
